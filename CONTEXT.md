# 投稿アプリ(学習用)

X(旧Twitter)風のミニ投稿アプリ。SES・S3+CloudFront・ページネーション・index・JOIN といったフルスタック開発の学習題材を自然に含むことを目的とする。

## Language

**投稿(Post)**:
ユーザーが書く最大280文字の短文コンテンツ。画像を任意で最大4枚添付できる。編集は不可、削除は物理削除で、削除できるのは投稿者本人のみ。
_Avoid_: ツイート、記事、メッセージ

**投稿画像(Post Image)**:
投稿に添付される画像。表示順を持つ。実体はS3(開発時はMinIO)に置き、DBはオブジェクトキーのみ持つ。
_Avoid_: 添付ファイル、メディア

**自己紹介(Bio)**:
ユーザーがプロフィールに任意で載せる短い紹介文(最大160文字程度)。
_Avoid_: プロフィール文、説明

**プロフィール画像(Avatar)**:
ユーザーが任意で1枚設定できる画像。タイムライン上の投稿に表示される。
_Avoid_: アイコン、顔写真

**ユーザー(User)**:
アプリに登録したアカウント。投稿・いいねの主体。メールアドレスで一意に識別され、パスワードと Google ログインの両方を同じアカウントに紐づけられる。
_Avoid_: アカウント、会員

**ユーザー名(Username)**:
ユーザーを表す一意の英数字の名前(X の @xxx に相当)。表示・検索に使い、ログインの識別子ではない。
_Avoid_: ログインID、アカウント名

**表示名(Display Name)**:
ユーザーが自由に設定できる名前。日本語可、一意でなくてよい。
_Avoid_: ニックネーム、ハンドルネーム

**カテゴリー(Category)**:
運営側があらかじめ用意するマスタデータとしての投稿の分類。各投稿はちょうど1つのカテゴリーを必ず持つ。ユーザーは作成できない。タイムラインの絞り込みに使う。
_Avoid_: タグ、ハッシュタグ、ジャンル

**いいね(Like)**:
ユーザーが投稿に付ける単一種別のリアクション。同じ投稿には1ユーザー1回までで、取り消しできる(トグル式)。
_Avoid_: お気に入り、リアクション

**検索ラボ(Search Lab)**:
アプリ内で唯一の検索ページ。検索条件(キーワード・対象・一致方法・カテゴリー)とページネーション設定(方式・件数)を自由に組み合わせて実行し、SQL 実行時間や EXPLAIN 結果を観察できる実験用ページ。ログインユーザー全員が使える。キーワードは空でも実行でき、件数を空にするとページネーションなしで取得する(サーバー側の安全上限あり)。
_Avoid_: 検索ページ、検証ページ

**プリセット(Preset)**:
検索ラボの検索条件の組み合わせをワンクリックでフォームにセットできる定義。フロントエンドのコードに直接定義し、アプリ上から追加・編集はできない。セットするだけで、検索は実行ボタンを押したときに行われる。
_Avoid_: テンプレート、保存済み検索

**タイムライン(Timeline)**:
全ユーザーの投稿が新しい順に流れる一覧。フォロー概念はなく全体で1本。カテゴリーで絞り込める。
_Avoid_: フィード、ホーム
